import axios from "axios";

// = api requests =
const useRequest = ({ setState }) => {
  // = assignment requests =
  // Create Form submit


  // return {
  //   postAssignment,
  //   putAssignment,
  //   deleteAssignment,
  //   patchSubmission,
  // };
};

export default useRequest;